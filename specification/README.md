# Specification Authority

The current constitutional product line is:

1. `GOALS.md` - current semantic-foundation work wave;
2. `INTENT.md` - domain direction;
3. `PRODUCT.md` - current product definition;
4. `requirements/` - not yet ratified;
5. downstream design and build tenants - not yet active.

The 2026-07-12 intent reprice resolves the earlier patchwork by defining
`odd_trading_eval` as a contextual trade-algebra construction and evaluation
product. The product is descriptive and evaluative before generative action.
The former forensic-attribution engine remains a projection and proving use
case inside the contextual algebra rather than a rival product definition.

These files are retained source material rather than current constitutional
authority:

- `BOOTSTRAP.md` - superseded seed and provenance;
- `cdm_trade_continuum_strategy.md` - strategy source;
- `cdm_trade_continuum_cuda_stack_proposal.md` - candidate compute design;
- `homeostatic_trade_continuum.md` - candidate control-model design.

Claims from source material become active only when derived through
`INTENT.md`, `PRODUCT.md`, requirements, and ratified design. File placement
under `specification/` does not promote candidate design into product law.

This sibling is a cross-reference, a candidate dependency rather than
ratified law:

- `odd_world_model` (`/Users/jim/src/apps/odd_world_model`) - intended
  derivation machinery for this product's contexts and treatments, building
  trade world models from existing trading-system code; WM ticket T-027
  (uplift to the current GTL/ABG/GLC line) is the carrier of that uplift.
